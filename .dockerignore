# Secrets — never send to Docker build context
.env
run.ps1

# Git metadata
.git

# IDE files
.idea/
.vscode/
*.iml

# OS files
.DS_Store
Thumbs.db

# NOTE: src/ and pom.xml are intentionally NOT excluded
# because the multi-stage Dockerfile needs them to build the JAR.
# The target/ folder is excluded because the builder stage creates it fresh.
target/
